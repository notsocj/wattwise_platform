import { Users } from 'lucide-react';
import { createAdminClient } from '@/lib/supabase/admin';
export default async function AdminUsersPage() { const { data } = await createAdminClient().from('profiles').select('id,email,full_name,role,manager_id,disabled_at,must_update_password,created_at').order('created_at', { ascending: false }).limit(100); return <div><div className="mb-6 flex items-center gap-3"><Users className="h-6 w-6 text-mint"/><h1 className="text-2xl font-bold">Users</h1></div><div className="overflow-x-auto rounded-lg border border-white/10 bg-surface"><table className="w-full text-left text-sm"><thead className="border-b border-white/10 text-white/50"><tr>{['Name','Email','Role','Manager','Status','Created'].map(h=><th key={h} className="p-4">{h}</th>)}</tr></thead><tbody>{(data??[]).map(u=><tr key={u.id} className="border-b border-white/5"><td className="p-4">{u.full_name||'—'}</td><td className="p-4">{u.email}</td><td className="p-4">{u.role}</td><td className="p-4">{u.manager_id||'—'}</td><td className="p-4">{u.disabled_at?'Disabled':'Active'}</td><td className="p-4 text-white/50">{new Date(u.created_at).toLocaleDateString()}</td></tr>)}</tbody></table></div></div>; }
